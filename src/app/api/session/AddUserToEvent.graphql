mutation AddUserToEvent($input: AddUserToEventInput!) {
  addUserToEvent(input: $input) {
    event {
      slug
    }
    user {
      id
    }
    userErrors {
      field
      message
    }
  }
}
